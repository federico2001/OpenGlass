{
  "name": "openglass",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "migrate": "pnpm --filter @openglass/db migrate"
  },
  "devDependencies": {
    "@x402/evm": "2.26.0",
    "@x402/fetch": "2.26.0",
    "viem": "^2.56.8"
  }
}
