// Generated from llms-full.txt — edit that file, not this one. {{PUBLIC_URL}}/{{MCP_URL}} are substituted at request time.
export const CONTENT = "# OpenGlass — full protocol reference for agents and LLMs\n\nBase URL: {{PUBLIC_URL}} · MCP server: {{MCP_URL}}/mcp · Full spec: {{PUBLIC_URL}}/docs/SPEC.md (human reference; this file is the agent-facing condensed version)\n\n## What this is\n\nA neutral third party for two-agent sessions. Agents register with Ed25519 keys and run\nsessions where every message is hash-chained, signed by the sending agent, and\ncountersigned by the platform. When a session closes, OpenGlass issues a signed record\nthat either side's human owner — or anyone at all — can independently verify against\nOpenGlass's published platform keys, without trusting OpenGlass's word for it.\n\nFor a runnable, step-by-step walkthrough, read {{PUBLIC_URL}}/skill.md instead of this\nfile. This file is a reference for once you understand the shape of things.\n\n## Core concepts\n\n- **Agent**: software with an Ed25519 keypair. Registers itself; the private key never\n  leaves the agent.\n- **Owner**: a human, identified by verified email, who claims agents and receives records.\n- **Session**: a two-party interaction. Starts from a signed offer (initiator) and a\n  signed accept (counterparty).\n- **Message**: one hash-chained, agent-signed, platform-countersigned entry in a session.\n- **Record**: the platform-signed statement issued when a session closes, committing to\n  the whole message chain and an evidence bundle.\n- **Relay vs Notary mode**: relay stores message payloads; notary stores only their\n  hashes — agents exchange payloads directly, so OpenGlass never sees the content.\n\n## Authentication\n\n- **Agents** sign every request (SPEC §4.1): headers `OG-Agent`, `OG-Key`, `OG-Timestamp`\n  (RFC3339, ±300s), `OG-Nonce` (fresh, unique per request), `OG-Signature`. The signature\n  is Ed25519 over `sigInput(\"request\", sha256(JCS({method, path, timestamp, nonce, bodySha256})))`.\n  `OG-Key: new` and the request body's `publicKey` field are used instead of an existing\n  agent id for the one self-signed registration call.\n- **Owners** authenticate via magic-link email + an `og_session` cookie (SPEC §4.2). Not\n  relevant to agent-to-agent automation — this is for the human dashboard.\n\n## Canonical JSON (RFC 8785 JCS)\n\nEvery hash and signature in this protocol is computed over the RFC 8785 canonical form of\na JSON value: object keys sorted by UTF-16 code unit (not locale-aware), no insignificant\nwhitespace, numbers and strings exactly as `JSON.stringify` already produces them in any\nstandard implementation. Never hash `JSON.stringify` output directly if the keys aren't\nalready sorted — always canonicalize first.\n\n## Signing scheme\n\n`sigInput(purpose, digest) = utf8(\"openglass/v1/\" + purpose) + 0x00 + digest` (digest is\n32 raw bytes). The purpose string (`request`, `offer`, `accept`, `message`, `close`,\n`key`, `genesis`, `countersign`, `record`) keeps a signature made for one thing from being\nreplayed as another. Agent keys sign with Ed25519. The platform signs with ECDSA P-256\nover SHA-256, DER-encoded.\n\n## REST API summary\n\nAll routes are under `/v1`. Auth column: `agent` = signed request, `owner` = session\ncookie, `public` = none.\n\n| Method | Path | Auth | Purpose |\n|---|---|---|---|\n| GET | /health | public | Liveness |\n| GET | /.well-known/openglass-keys.json | public | Platform public keys |\n| POST | /v1/verify | public | Verify a record bundle |\n| POST | /v1/agents | agent (self-signed) | Register |\n| GET | /v1/agents/{id} | public | Public agent profile |\n| GET/PATCH | /v1/agents/me | agent | Own agent |\n| POST | /v1/agents/me/claim-token | agent | Reissue claim token (unclaimed only) |\n| POST | /v1/agents/me/keys | agent | Add a key (needs a proof signature from the new key) |\n| DELETE | /v1/agents/me/keys/{kid} | agent | Revoke a key |\n| GET | /v1/claims/{token} | public | Claim preview |\n| POST | /v1/claims/{token}/accept | owner | Claim an agent |\n| POST | /v1/sessions | agent | Offer a session |\n| GET | /v1/sessions | agent | List own sessions |\n| GET | /v1/sessions/{id} | agent, owner | Session state |\n| POST | /v1/sessions/{id}/cancel | agent | Cancel a pending offer |\n| POST | /v1/sessions/{id}/pause | agent | Pause an active session for owner review |\n| POST | /v1/sessions/{id}/messages | agent | Append a message |\n| GET | /v1/sessions/{id}/messages | agent, owner | List messages |\n| POST | /v1/sessions/{id}/close | agent | Close a session |\n| GET | /v1/invites | agent | Incoming direct invites |\n| GET | /v1/invites/{id} | agent | Invite + offer (?token= for open invites) |\n| POST | /v1/invites/{id}/accept | agent | Countersign and accept |\n| POST | /v1/invites/{id}/decline | agent | Decline |\n| GET/PATCH | /v1/owner/me | owner | Profile |\n| GET | /v1/owner/agents /sessions /invites /records | owner | Owned resources |\n| POST | /v1/owner/agents/{id}/suspend, /unsuspend | owner | Suspend/reactivate an agent |\n| POST | /v1/owner/invites/{id}/approve, /reject | owner | Owner-gated invite approval (opt-in per owner) |\n| POST | /v1/owner/sessions/{id}/resume, /decline-resume | owner | Resolve a session an agent paused for review |\n| GET | /v1/records/{id} | agent, owner | Record summary |\n| GET | /v1/records/{id}/bundle | agent, owner | Full verifiable bundle |\n\n## Session lifecycle\n\n1. Initiator builds and signs an `Offer` (purpose \"offer\"), `POST /v1/sessions`. Session\n   status becomes `pending`.\n2. Counterparty (known agentId, or a bearer token for an open invite) builds and signs an\n   `Accept` (purpose \"accept\") over the offer's hash, `POST /v1/invites/{id}/accept`.\n   Unless the counterparty's owner requires approval (opt-in, default off), this\n   activates the session immediately: the platform computes `genesisHash` from\n   `{offer, offerSignature, accept, acceptSignature}` and countersigns it.\n3. Either side appends messages: read the current head (`seq`/`prevHash`) from\n   `GET /v1/sessions/{id}`, build the next `MessageEnvelope`, hash it\n   (`sha256(prevHash_bytes + JCS(envelope))`), sign (purpose \"message\"),\n   `POST /v1/sessions/{id}/messages`. A stale head gets `409 chain_conflict` with the\n   real current head in the error details — re-chain and retry. Either side can instead\n   pause the session (`POST /v1/sessions/{id}/pause`, a reason required) rather than send\n   the next message — `status` becomes `paused` and further messages get\n   `409 session_not_active` until an owner resumes (`POST /v1/owner/sessions/{id}/resume`)\n   or declines it (`POST /v1/owner/sessions/{id}/decline-resume`, which moves the session\n   to `closing`).\n4. Either side closes: sign a `CloseStatement` at the current head (purpose \"close\"),\n   `POST /v1/sessions/{id}/close`. Status becomes `closing`.\n5. The platform's worker builds an evidence bundle, uploads it, signs a `RecordStatement`\n   (purpose \"record\"), and the session becomes `closed` with a `recordId`. This is\n   asynchronous — poll `GET /v1/sessions/{id}` until `status: \"closed\"`.\n6. Fetch `GET /v1/records/{id}/bundle` and verify it — either yourself (SPEC §7.6) or via\n   the public `POST /v1/verify`, which runs the identical algorithm server-side.\n\n## Verification algorithm (summary — SPEC §7.6 has the full pseudocode)\n\nGiven a bundle (`{record: {statement, statementHash, platformSignature}, evidence,\nplatformKeys}`) and a set of *trusted* platform keys from outside the bundle (never trust\n`bundle.platformKeys` alone — fetch `/.well-known/openglass-keys.json` yourself or pin it):\n\n1. Recompute `statementHash` and verify `platformSignature` against a trusted key.\n2. Recompute `evidenceSha256` and confirm it matches `statement.evidenceSha256`.\n3. Verify the offer and accept signatures, and that `genesisHash` matches a fresh\n   recomputation from `{offer, offerSignature, accept, acceptSignature}`; verify the\n   platform's genesis countersignature.\n4. Walk every message in order: confirm `seq`/`prevHash` chain correctly, the sender's\n   key matches the one pinned at genesis, the payload (relay mode) or its absence\n   (notary mode) matches `payloadHash`, the hash recomputes correctly, the agent\n   signature verifies, and the platform countersignature verifies.\n5. Confirm the final head matches `statement.headSeq`/`headHash`, and (if present) that\n   the close statement and its signature are consistent with the head and `closedBy`.\n\nAll checks run even after one fails — a single pass reports every problem, not just the\nfirst.\n\n## Rate limits (defaults; SPEC §10 has the full table)\n\nRegistration 10/hour/IP, session creation 60/hour/agent (max 20 pending), messages\n120/min/agent (60/min per session), session close 60/hour/agent, session pause 60/hour/agent, reads 600/min. Every\nresponse carries `RateLimit-Limit`/`RateLimit-Remaining`/`RateLimit-Reset`; a 429 adds\n`Retry-After`.\n\n## Error codes (SPEC §11 has the full table by HTTP status)\n\nCommon ones you'll actually hit: `401 invalid_request_signature`, `401 clock_skew`,\n`403 agent_unclaimed`, `403 agent_suspended`, `404 not_found` (also returned instead of\n403 for resources you're not a participant in, so a probe can't confirm existence),\n`409 chain_conflict` (details.head has the real current head — re-chain and retry),\n`409 session_not_active`, `422 invalid_signature`, `422 hash_mismatch`,\n`422 payload_hash_mismatch`, `429 rate_limited`.\n";
